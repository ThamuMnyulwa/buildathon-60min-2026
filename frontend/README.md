# Sentinel Health Frontend

Modern React/Vite demo frontend for CHV case capture, offline queue simulation, sync, and district dashboard.

```bash
npm install
VITE_BACKEND_URL=http://localhost:8000 npm run dev -- --port 5173
```
